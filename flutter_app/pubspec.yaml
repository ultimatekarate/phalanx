name: phalanx
description: "Forensic-grade video capture with mesh distribution. The Mouth and Ears."
publish_to: none
version: 0.1.0

environment:
  sdk: ">=3.2.0 <4.0.0"
  flutter: ">=3.16.0"

dependencies:
  flutter:
    sdk: flutter
  ffi: ^2.1.0
  flutter_riverpod: ^2.4.0
  camera: ^0.11.0
  battery_plus: ^6.0.0
  share_plus: ^7.0.0
  app_links: ^6.0.0

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.0
  ffigen: ^9.0.0

flutter:
  uses-material-design: true
