[package]
name = "phalanx-dashboard"
version = "0.1.0"
edition = "2021"

[dependencies]
tokio = { version = "1.49", features = ["full"] }
crossterm = "0.29"
ratatui = "0.30"
anyhow = "1.0"

# Local Core Dependency (with Simulation features enabled)
phalanx-core = { path = "../phalanx-core", features = ["sim"] }