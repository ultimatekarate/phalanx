/// Shared state ether where simulated nodes meet.
/// Holds the in-memory mesh topology, virtual channels between nodes,
/// and the chaos injection state for each link.
///
/// TODO: Implement node registry, virtual transport channels, and topology snapshots.
pub struct SimulationWorld;
